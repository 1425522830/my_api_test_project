登录所需 Token 为一次性，抓包获取的 Token 已失效，需要先 GET 拿到新 Token，再发起登录 POST。

疑问 1：服务器不能提供一个单独接口直接获取 token 吗？
没有！Flarum 原生没有 /api/get-csrf 这类独立接口。
设计就是：访问页面渲染时生成 token，强制你加载页面才能拿到。
这是网站本身的防护设计，防止纯 API 脚本轻易发起请求。

疑问 2：能不能用 json.loads 直接解析？
不行。
token 只是一大段 script 脚本里的一小片，不是完整独立的 JSON 字符串，无法直接 json 解析。
示例片段：

有些 Web 后端 API，不是登录拿到 cookie 就可以随便调用全部接口。
会校验访问顺序：必须先请求前端页面，再调用后端 JSON 接口。
抓包看浏览器的完整请求顺序，而不是只复制单独的 api 请求，这是接口自动化经常踩的坑。

Flarum 这个特殊环境返回的 JSON 结构非常规。
如果按照常规（你之前的代码），它会去找 resp_json["data"]["id"]，这必然崩溃。
我们加了防御逻辑把它打印出来后，发现它直接在最外层返回了 userId: 575。



彻底攻克了“身份认证”难关（CSRF+Cookie）：
找到了 Flarum 特有的“一次性 Token”机制，放弃了全局缓存，改用每次都从首页正则提取 Token；同时精准模拟了登录时游客 Cookie 的处理，巧妙绕开了底层 post() 自动刷新 Token 导致 401 的死循环。

告别硬编码，实现动态数据提取：
在 conftest.py 的 login_client 夹具中，直接从登录响应里提取了真实结构中的 userId（而不是常规的 data.id，这就是以抓包为准的体现）。不仅解决了 404 问题，也实现了“换账号测试不用改代码”。

严格遵守 HTTP 语义，以抓包为准：
经历了由于请求方式错误导致的 405 报错。确定了更新用户资料（如改昵称、通知设置）必须用 PATCH，删除会话用 DELETE，而此论坛的某些接口竟然用 POST。学会不猜规则，只看网络面板的实际请求。

